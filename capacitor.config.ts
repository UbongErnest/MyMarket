import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.westudentmarketplace.app',
  appName: 'WE Student Marketplace',
  webDir: 'dist'
};

export default config;
